import React, { useEffect, useState, useContext } from 'react';

import Box from '@mui/material/Box';

const Editor = ({}) => {
  return <Box sx={{ width: '100%', px: 1 }}>Editor</Box>;
};

export default Editor;
